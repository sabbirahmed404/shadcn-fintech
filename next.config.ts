import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // If the pnpm-workspace warning persists, you can try setting this at the top-level or ignore it.
};

export default nextConfig;
